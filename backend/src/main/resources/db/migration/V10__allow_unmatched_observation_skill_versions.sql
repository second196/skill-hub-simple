-- Observation skill identity is the observed (slug, version label) pair.
-- Local-only and unversioned skill steps are valid after this contract change.
-- V9 remains immutable because it may already be recorded in flyway_schema_history.
SELECT 1;
